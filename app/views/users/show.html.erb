<!-- TODO Change route to dynamically get params! -->

<%= form_tag("/users/#{@user_id}/locations") do %>
 
	<%= render 'welcome/form' %>
<% end %>


<% if @logged_in %>
	<% @trips.each do |trip| %>
		<%= trip.location.name %>
		<br>
	<% end %>
<% end %>